{
  "name": "06-noc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "docker:test": "docker compose -f docker-compose.test.yml --env-file .env.test up -d",
    "test:watch": "pnpm docker:test && cross-env NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "pnpm docker:test && cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.2.3",
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.6",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.10",
    "cron": "^4.4.0",
    "dotenv": "^17.3.1",
    "env-var": "^7.5.0",
    "json-server": "1.0.0-beta.6",
    "mongoose": "^9.2.1",
    "nodemailer": "^8.0.1"
  }
}
